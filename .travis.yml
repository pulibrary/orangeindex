language: ruby
rvm:
  - jruby-1.7.23 # JRuby most recent 1.7 release
jdk:
  - oraclejdk8
script: bundle exec rspec spec

branches:
  only:
    - development
notifications:
  email:
    recipients:
      - "tampakis@princeton.edu"
      - "jstroop@princeton.edu"
      - "kr2@princeton.edu"
    on_success: "change"
    on_failure: "always"
    template:
      - "%{repository}//%{branch}@%{commit} by %{author}: %{message} - %{build_url}"